140050002

There is .apk file which can be installed. Open it and run.

Note :

While adding course; "true" status displayed during alert does NOT mean course is successfully added. It means DbHandler returned status "true" if query processing happened without error.